# 研究日志：2026-09-02

## 今日完成

- 学习四步相移干涉的成像模型和相位解调公式。
- 理解通过图像差分消去背景项与调制度的过程。
- 理解 `atan2` 的几何意义、象限判断和包裹输出范围。
- 学习由相位恢复光程差、波前和反射表面高度的方法。
- 分析 $A$、$B$、$\phi$ 和 $\delta_k$ 的物理来源及影响因素。
- 明确传统顺序相移在动态情况下被破坏的基本假设。

## 已掌握的核心公式

$$
\phi=\mathrm{atan2}(I_4-I_2,I_1-I_3)
$$

$$
A=\frac{I_1+I_2+I_3+I_4}{4}
$$

$$
B=\frac12\sqrt{(I_1-I_3)^2+(I_4-I_2)^2}
$$

$$
\Delta=\frac{\lambda}{2\pi}\phi_{\mathrm{unwrapped}}
$$

$$
h=\pm\frac{\lambda}{4\pi}\phi_{\mathrm{unwrapped}}
$$

## 本课易混淆点

- `atan2` 的参数顺序是先正弦分量、后余弦分量，即 `atan2(y, x)`。
- 不同资料的相移方向与图像编号可能不同，解调公式的符号也会随之变化。
- 四步法恢复的是包裹相位，不能直接用于大范围连续高度重建。
- 反射表面高度和波前光程差相差因子2，因为反射光经历往返传播。
- 被测表面移动主要破坏的是四次采集期间 $\phi$ 保持不变的假设。

## 补充学习：二维相位展开

- 理解包裹相位与真实相位之间相差整数个 $2\pi$。
- 明确算法只使用测得的包裹相位，不会使用未知的真实相位。
- 理解对相邻包裹相位差再次包裹，是为了消除跨越 $\pm\pi$ 边界产生的虚假跳变。
- 明确 $g_x、g_y$ 只是局部相位差主值，只有在相邻真实相位变化小于 $\pi$ 时才等于真实局部差值。
- 通过 $2\times2$ 相位图理解二维路径一致性和噪声造成的路径矛盾。
- 理解最小二乘法通过全局折中，使相位梯度误差平方和最小。
- 初步了解加权最小二乘法、泊松方程和动态时间相位展开。

## 下一步

- 使用 Python 生成四幅理想相移干涉图。
- 用 `numpy.arctan2` 恢复包裹相位并进行误差验证。
